# 恋爱纪念册 · 概要设计（合并概设 + 概要 + 复杂模块详设）

> 版本：v1　日期：2026-08-13
> 定位：本文件是开发的**主参考文档**。功能点的逐条清单见配套 `恋爱纪念册-功能清单.xlsx`（63 个功能点，表头：模块/功能/功能点/描述/备注）；本文在此之上补齐架构、数据模型、接口契约与 3 个复杂模块的详细设计。
> 目标：可「添加到主屏幕」的 iOS PWA，数据全量上云（Supabase），设计从简、偏暖。

---

## 1. 产品定位
一款面向**情侣二人**的移动端 PWA（伪原生 App，添加到主屏幕即用）。记录两人共同经历、增进日常陪伴，强调「温度」而非功能堆砌。
- 无原生推送（已明确放弃提醒功能）。
- 数据全量上云（Supabase Postgres + Storage），实时同步，多设备一致。
- 取代旧版「本地 IndexedDB + PeerJS P2P」架构。

## 2. 目标用户与使用场景
- 情侣两人，均在大陆，**可科学上网**（Supabase 官方托管 `supabase.co` 可达）。
- 主要入口：iOS Safari / 微信内置浏览器 → 「添加到主屏幕」。
- 核心场景：每天打开看「我们在一起多久」、写日记、聊几句、记录一起想做的事。

## 3. 设计原则
- **简单**：底部仅 4 个 Tab；每个页面只做一个主操作，表单极简。
- **暖**：粉系主色、圆角卡片、手写感字体、柔和微交互（淡入/心跳）。
- **真实优先**：用两人真实照片与素材，避免占位图（冰冷感头号来源）。
- **拟人化文案**：把「添加」改成「记一笔 / 写下来 / 加一个愿望」，数字变成有温度的话。

## 4. 信息架构与导航
底部 **4 个 Tab**：`首页 · 记忆 · 聊天 · 我的`

| Tab | 角色 | 包含 |
|---|---|---|
| 首页 | 情感中枢 | 天数、倒计时、今日情话、今日速览、回忆入口 |
| 记忆 | 共同经历 | 情侣日记、日记日历、照片墙、回忆时间轴 |
| 聊天 | 实时连接 | 即时聊天、在线/输入中/已读 |
| 我的 | 管理入口 | 身份配对、生活（纪念日/计划/任务/打卡）、共娱（影视）、设置（主题/备份/关于） |

> 其余功能全部作为入口收进「我的」页，底部永不增加 Tab，保持简单。

## 5. 功能清单索引（详见 xlsx）
完整 63 个功能点见 `恋爱纪念册-功能清单.xlsx`。下表为「模块 → 所属表 → 是否复杂」的索引，便于开发定位：

| 模块 | 功能 | 主要数据表 | 复杂度 |
|---|---|---|---|
| 首页 | 天数/倒计时/情话/速览/入口 | couples, anniversaries, tasks, checkins | 低（只读聚合） |
| 记忆 | 情侣日记/日历/照片墙/时间轴 | diary_entries, diary_photos, 视图 | 低（CRUD+聚合） |
| 聊天 | 即时聊天/状态 | messages, Storage | **高（见 §12.2）** |
| 我的 | 身份配对 | profiles, couples | **高（见 §12.1）** |
| 我的 | 纪念日/计划/任务/打卡/影视 | 各表 | 低（CRUD） |
| 我的 | 主题/备份/关于 | 本地偏好 / JSON 导出 | 低 |

> 「数据同步」不是一个独立功能，而是所有表的统一机制：**Supabase 即唯一真相源，两人各自 select + 订阅增量**（见 §12.3）。故不单列模块。

## 6. 重点页面布局要点
### 首页（情感锚点）
- Hero：一张两人合照 + 「我们在一起 N 天」+ 起始日期（大字号）。
- 纪念日倒计时：粉色卡片，带小心形，制造期待。
- 今日情话：白卡 + 手写感字体，可「换一句」。
- 今日速览：今日任务 + 打卡状态（速览，详细在「我的」）。
- 最新回忆入口：跳日记/照片/时间轴。

### 记忆（共同经历）
- 情侣日记：时间倒序列表 + 写/详情；双作者用不同色点与头像区分。
- 日记日历：月历标记有日记的日期（小粉点），点击跳当天。
- 照片墙：**仅自动聚合日记中的图片**，不提供主动上传入口。
- 回忆时间轴：竖向时间轴，聚合日记/纪念日/影视/计划/打卡事件。

### 聊天（实时连接）
- 消息流（文+图）+ 输入框；在线小绿点、对方「输入中…」、已读标记。
- 进页先拉历史最近 N 条，再靠 WebSocket 订阅增量。

### 我的（管理入口，分组）
- 身份：身份配对（昵称/头像/代表色 + 配对码）。
- 生活：纪念日、未来计划、日常任务、日常打卡。
- 共娱：影视记录。
- 设置：主题、备份导出、关于我们。

## 7. 技术栈
- 纯前端，`<script>` 直引，**无构建框架**（契合现有结构、易维护）。
- Supabase JS（CDN 引入：`https://cdn.jsdelivr.net/npm/@supabase/supabase-js`）。
- 认证：Supabase Auth **匿名登录**（Anonymous Sign-In），每设备一个 auth user，profile 绑定 couple。
- 图片：Supabase Storage（`avatars/`、`diary/` 两个 bucket）。
- PWA：`manifest.json` + Service Worker（缓存应用壳 + 离线兜底）。
- 文本：Postgres。

## 8. 数据模型（精确表结构 + RLS）
所有表按 `couple_id` 隔离，仅同一 couple 的两人可读写。约定：`profiles.id = auth.uid()`。

### 8.1 表结构
```sql
-- 情侣对（配对码绑定 + 起始日）
create table couples (
  id          uuid primary key default gen_random_uuid(),
  pair_code   text unique not null,        -- 6位随机码，用于绑定
  start_date  date not null,               -- 恋爱开始日（首次引导填）
  created_at  timestamptz default now()
);

-- 两人身份（id 即 auth.uid()）
create table profiles (
  id          uuid primary key default auth.uid(),
  couple_id   uuid references couples(id) not null,
  nickname    text not null,
  avatar_url  text,                        -- Storage path: avatars/{id}.jpg
  color       text not null,               -- 代表色 hex，如 #E86A92
  created_at  timestamptz default now(),
  -- 绑定后 couple_id 不可改（见 RLS）
);

-- 纪念日（支持农历）
create table anniversaries (
  id          uuid primary key default gen_random_uuid(),
  couple_id   uuid references couples(id) not null,
  title       text not null,
  date        date not null,              -- 公历日期
  is_lunar    boolean default false,       -- true 时 date 为农历换算后存储的公历日期
  type        text,                        -- 恋爱纪念/生日/初约...
  created_at  timestamptz default now(),
  is_deleted  boolean default false
);

-- 未来计划（体验目标）
create table plans (
  id          uuid primary key default gen_random_uuid(),
  couple_id   uuid references couples(id) not null,
  title       text not null,
  description text,
  type        text,                        -- 旅行/美食/生活目标/体验
  status      text default 'idea',         -- idea(想做)/doing(进行中)/done(完成)
  created_at  timestamptz default now(),
  is_deleted  boolean default false
);

-- 日常任务（家务例行，可指派执行人）
create table tasks (
  id          uuid primary key default gen_random_uuid(),
  couple_id   uuid references couples(id) not null,
  title       text not null,
  assignee_id uuid references profiles(id),  -- 执行人（两人之一）
  status      text default 'todo',         -- todo/.done
  created_at  timestamptz default now(),
  is_deleted  boolean default false
);

-- 日常打卡（互动：早安/晚安/想你 + 连续天数）
create table checkins (
  id          uuid primary key default gen_random_uuid(),
  couple_id   uuid references couples(id) not null,
  type        text not null,              -- morning/evening/miss
  who         uuid references profiles(id) not null,  -- 谁打的卡
  date        date default current_date,   -- 打卡日期（一人一天一类型唯一）
  note        text,                        -- 可附一句话
  streak      int default 1,               -- 连续天数（由写入逻辑维护）
  created_at  timestamptz default now(),
  unique (couple_id, type, who, date)
);

-- 情侣日记
create table diary_entries (
  id          uuid primary key default gen_random_uuid(),
  couple_id   uuid references couples(id) not null,
  author_id   uuid references profiles(id) not null,  -- 作者（双作者区分）
  body        text not null,
  created_at  timestamptz default now(),
  is_deleted  boolean default false
);

-- 日记图片（照片墙唯一来源，不主动上传）
create table diary_photos (
  id          uuid primary key default gen_random_uuid(),
  entry_id    uuid references diary_entries(id) not null,
  url         text not null,              -- Storage path: diary/{id}.jpg
  created_at  timestamptz default now()
);

-- 影视记录
create table movies (
  id          uuid primary key default gen_random_uuid(),
  couple_id   uuid references couples(id) not null,
  title       text not null,
  poster      text,                        -- Storage path 或外链
  rating      numeric(2,1) check (rating between 0 and 5),  -- 双人共用？见说明
  watched     boolean default false,        -- 想看=false / 已看=true
  review      text,                        -- 观后感
  created_at  timestamptz default now(),
  is_deleted  boolean default false
);

-- 聊天消息
create table messages (
  id          uuid primary key default gen_random_uuid(),
  couple_id   uuid references couples(id) not null,
  sender_id   uuid references profiles(id) not null,
  body        text,                        -- 文字内容（图片消息时可为空）
  kind        text default 'text',         -- text/image
  image_url   text,                        -- Storage path（kind=image 时）
  created_at  timestamptz default now(),
  read_at     timestamptz                   -- 已读时间（对方打开会话时写）
);

-- 回忆时间轴：用视图聚合，不建表
create view timeline as
  select 'diary' as source, id, couple_id, created_at, body as title from diary_entries where not is_deleted
  union all
  select 'anniversary', id, couple_id, date::timestamptz, title from anniversaries where not is_deleted
  union all
  select 'movie', id, couple_id, created_at, title from movies where not is_deleted
  union all
  select 'plan', id, couple_id, created_at, title from plans where not is_deleted
  union all
  select 'checkin', id, couple_id, created_at, type from checkins;
```

> 影视 `rating` 说明：xlsx 要求「双方评分与观后感」。简洁方案为 `movies` 表加 `rating_a`、`rating_b`、`review_a`、`review_b` 两列分别存两人；或拆 `movie_ratings` 子表。初版建议用两列方案（`rating_a/rating_b numeric(2,1)`、`review_a/review_b text`），避免额外 join。本文档以单列 `rating/review` 为占位，落地时按两列实现。

### 8.2 索引
```sql
create index on profiles (couple_id);
create index on messages (couple_id, created_at desc);
create index on diary_entries (couple_id, created_at desc);
create index on checkins (couple_id, type, who, date);
create index on diary_photos (entry_id);
```

### 8.3 RLS（行级安全，核心）
通用模式：所有业务表开启 RLS，仅允许「同 couple」访问。
```sql
alter table couples enable row level security;
alter table profiles enable row level security;
-- ... 其余表同理

-- 辅助函数：取当前用户所属 couple_id
create or replace function my_couple()
returns uuid language sql stable as $$
  select couple_id from profiles where id = auth.uid()
$$;

-- 以 messages 为例（其余表把表名/字段替换即可）
create policy "同couple可读消息" on messages
  for select using (couple_id = my_couple());

create policy "仅本人可发消息" on messages
  for insert with check (couple_id = my_couple() and sender_id = auth.uid());

create policy "仅发送者可改已读" on messages
  for update using (couple_id = my_couple())
  with check (couple_id = my_couple());

-- profiles：禁止改 couple_id（绑定锁定）
create policy "本人资料可读" on profiles
  for select using (couple_id = my_couple());
create policy "本人资料可写" on profiles
  for insert with check (id = auth.uid());
create policy "本人资料可改(锁定couple)" on profiles
  for update using (id = auth.uid())
  with check (id = auth.uid() and couple_id = (select couple_id from profiles where id = auth.uid()));
```

### 8.4 Realtime 发布
```sql
alter publication supabase_realtime add table messages;
alter publication supabase_realtime add table diary_entries;
alter publication supabase_realtime add table plans;
alter publication supabase_realtime add table tasks;
alter publication supabase_realtime add table checkins;
alter publication supabase_realtime add table anniversaries;
alter publication supabase_realtime add table movies;
```

## 9. 实时与同步接口契约
客户端统一用 `supabase-js`：
```js
const sb = supabase.createClient(URL, ANON_KEY);
```

### 9.1 聊天订阅
```js
const channel = sb.channel('room:' + coupleId)
  .on('postgres_changes',
      { event: 'INSERT', schema: 'public', table: 'messages',
        filter: `couple_id=eq.${coupleId}` },
      (payload) => renderMessage(payload.new))
  .subscribe();
// 在线状态
channel.track({ user_id: auth.uid(), online_at: new Date().toISOString() });
// 输入中（不发库，走 broadcast）
channel.send({ type: 'broadcast', event: 'typing', payload: { sender: auth.uid() } });
// 离开页面必须清理
sb.removeChannel(channel);
```

### 9.2 数据同步（各模块通用）
- 进入模块：`select ... where couple_id = eq.{coupleId} and not is_deleted order by created_at desc limit 50`。
- 订阅增量：对该表 `postgres_changes` INSERT/UPDATE/DELETE，按 `couple_id` 过滤，收到即更新本地 UI。
- 无推送：关页面即断，重开重新 `select` 全量最新。

## 10. 模块依赖关系
```
profiles ──┐
           ├─→ couples（一对）
           │
messages / diary_entries / diary_photos / anniversaries /
plans / tasks / checkins / movies  ── 全部 FK → couples(couple_id)
                                      全部受 RLS(my_couple()) 隔离
timeline(view) ── 聚合上述事件表
```
- 依赖根：`couples` + `profiles`（身份配对，必须最先建，见 §12.1）。
- 其余所有模块依赖「已配对」状态；未配对时 App 停留在配对引导页。

---

## 11. 重点页面 → 数据调用速查
| 页面 | 读取 | 写入 |
|---|---|---|
| 首页 | couples(start_date), anniversaries(下个), tasks(今日), checkins(今日), 最新 diary | 无 |
| 记忆·日记 | diary_entries + diary_photos | insert/update/delete diary_entries(+photos) |
| 记忆·照片墙 | diary_photos（join diary_entries） | 无（只读） |
| 记忆·时间轴 | timeline 视图 | 无（只读） |
| 聊天 | messages 历史 + 订阅 | insert messages, update read_at |
| 我的·各入口 | 对应表 select | insert/update/delete 对应表 |

---

## 12. 复杂模块详细设计

### 12.1 身份配对与绑定
**目标**：两设备通过「配对码」绑成同一个 `couple`，各自成为 `profiles`，之后所有数据按 `couple_id` 隔离。
**前置**：Supabase Auth 匿名登录已开启（`signInAnonymously`）。

**时序（A 生成，B 加入）**
```
[A 设备]                         [B 设备]                         [Supabase]
 auth.signInAnonymously() ─────────────────────────────────────► 匿名 user_A
 生成 6位 pair_code (随机)
 insert couples(pair_code,start_date) ───────────────────────► couple_id=C
 insert profiles(id=user_A, C, nickname_A, color_A)
 ┌─ 展示 pair_code，请 B 输入 ─┐
 │                                                            auth.signInAnonymously() ──► user_B
 │                              B 输入 pair_code ─────────────►
 │                              select couple_id from couples
 │                                where pair_code = ? ────────► 返回 C
 │                              insert profiles(id=user_B, C,
 │                                nickname_B, color_B) ───────►
 │                              （此后 B 的全部查询走 my_couple()=C）
 ▼                                                            ▼
 双方均进入主页（已配对状态）                               RLS 仅允许 C 内两人互访
```

**步骤与边界**
1. A 点「创建我们的空间」→ 匿名登录 → 生成 `pair_code`（6 位，建议大写字母+数字，避免易混字符）→ 写 `couples` → 写 `profiles`（A）。
2. A 把 `pair_code` 通过任意方式发给 B（不自动传，避免额外通道）。
3. B 点「加入 TA」→ 匿名登录 → 输入 `pair_code` → 后端校验存在且 `profiles` 中该 couple 人数 < 2 → 写 `profiles`（B）。
4. **绑定锁定**：`profiles.couple_id` 写入后不可改（RLS 禁止 update couple_id）。如需「解绑重来」，提供「退出空间」= 删除本设备 `profiles` + 匿名 user（couples 保留直到双方都退出，由应用层判断）。

**错误码 / 异常**
- `pair_code` 不存在 → 提示「配对码无效」。
- `pair_code` 已绑定两人 → 提示「该空间已满，请创建新空间」。
- 匿名登录失败（网络/VPN）→ 提示「请检查网络后重试」。

**首启引导**：`start_date`（恋爱起始日）在 A 创建时必填；`nickname`/`color` 两人各填。引导完成后写入，不可在「我的」外随意改。

### 12.2 即时聊天（实时）
**表**：`messages`（见 §8.1）。**机制**：`postgres_changes` + `Presence` + `Broadcast`。

**发送文字**
```js
await sb.from('messages').insert({
  couple_id, sender_id: auth.uid(), body, kind: 'text'
});
```
**发送图片**
```js
const path = `chat/${coupleId}/${Date.now()}.jpg`;
await sb.storage.from('chat').upload(path, file);
await sb.from('messages').insert({
  couple_id, sender_id: auth.uid(), kind: 'image', image_url: path
});
```
**接收（订阅）**：见 §9.1，INSERT 事件 → `renderMessage(new)`，追加到消息流并滚到底。

**历史加载**：进聊天页先
```js
const { data } = await sb.from('messages')
  .select('*').eq('couple_id', coupleId)
  .order('created_at', { ascending: false }).limit(50);
renderHistory(data.reverse());
```
再建立 §9.1 的订阅，处理进入页面后新到的消息。

**在线状态（Presence）**
```js
channel.on('presence', { event: 'sync' }, () => {
  const state = channel.presenceState();
  const otherOnline = Object.keys(state).some(k => k !== auth.uid());
  setOnlineDot(otherOnline);
});
```

**输入中（Broadcast，不发库）**
```js
// 我方输入时
channel.send({ type: 'broadcast', event: 'typing', payload: { sender: auth.uid() } });
// 接收
channel.on('broadcast', { event: 'typing' }, () => showTypingBubble());
```
输入停顿 2s 后停止显示。

**已读标记**
- 打开会话即批量写：
```js
await sb.from('messages')
  .update({ read_at: new Date().toISOString() })
  .eq('couple_id', coupleId)
  .neq('sender_id', auth.uid())
  .is('read_at', null);
```
- 注意：无推送，已读 = 「对方打开会话时才算」，非「消息到达对方设备即亮」。

**退出清理（防连接累积，生产头号坑）**
```js
// 离开聊天页 / 切后台 / 卸载组件时
sb.removeChannel(channel);
```

**边界**
- 图片先传 Storage 再写 `messages`，失败则提示重发。
- 断网时发送按钮禁用或本地乐观显示「发送中」，重连后补发（初版可简化为禁用发送并提示）。

### 12.3 数据同步合并
**核心认知**：本项目的「同步」不是 P2P 合并，而是 **Supabase Postgres 即唯一真相源**——两人各自 `select` + 订阅增量，任何一方写入，另一方通过 `postgres_changes` 实时收到并刷新 UI。无需客户端做冲突合并。

**首次进入某模块**
```js
select ... where couple_id = eq.{C} and not is_deleted
order by created_at desc limit 50;   -- 首屏
```
**增量**
```js
.on('postgres_changes',
    { event: '*', schema: 'public', table: '<模块表>',
      filter: `couple_id=eq.${C}` },
    (p) => {
      if (p.eventType === 'INSERT') addItem(p.new);
      if (p.eventType === 'UPDATE') updateItem(p.new);
      if (p.eventType === 'DELETE') removeItem(p.old);
    });
```

**冲突处理**
- 两人各写「自己的」记录：`diary_entries.author_id`、`tasks.assignee_id`、`checkins.who`、`messages.sender_id` 天然区分作者，不冲突。
- 共享对象极少（`couples.start_date`、少量 `anniversaries`）：采用 **last-write-wins**，无需 CRDT/OT。
- 删除用 `is_deleted` 标记（软删除），订阅 `UPDATE` 时 `is_deleted=true` 即从 UI 隐藏；不物理删，便于历史与可逆。

**离线兜底**
- 断网时：UI 展示上次 `select` 的缓存（IndexedDB 存一份最近快照），发送类操作禁用或本地乐观。
- 重连：重新 `select` 全量最新 + 重新订阅，UI 校正。
- 初版可只做「断网提示 + 重连自动刷新」，不做本地写队列（因双人低频、且需 VPN）。

**照片墙同步**：`diary_photos` 随 `diary_entries` 实时同步；照片墙只读聚合，无需单独同步逻辑。

---

## 13. 阶段排期
- **基础（必先做）**：Supabase 建表 + RLS + 匿名登录 + 身份配对（§12.1）。
- **打通 4 Tab**：聊天（§12.2）→ 首页聚合 → 记忆（日记/日历/照片墙/时间轴）→ 我的各入口。
- **稳健增强**：离线兜底（§12.3）、主题、备份导出、关于我们。
- 建议落地顺序：建表/RLS → 配对 → 聊天最小可跑 demo → 首页 → 记忆 → 我的。

## 14. 风险与备注
- **依赖 VPN**：两人需科学上网才能连 `supabase.co`；一方关 VPN 则断。
- **无推送**：聊天仅页面活跃时实时，已按用户要求放弃提醒；已读为「打开会话才算」。
- **iOS PWA 限制**：无原生通知；Safari 对部分 API 有限制（如文件系统写）。
- **微信 webview**：需系统级 VPN 覆盖 webview 流量，否则连不上 Supabase。
- **照片墙策略**：仅抓取日记中的图片，不主动上传，降低复杂度与隐私面。
- **匿名用户留存**：Supabase 匿名 user 默认长期保留，简单项目可接受；若担心，可在「退出空间」时显式删 user。
- **影视评分**：落地时按 `rating_a/rating_b` 两列实现双人评分（见 §8.1 说明）。
