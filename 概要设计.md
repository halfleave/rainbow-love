# 恋爱纪念册 · 概要设计（定稿 v2）

> 版本：v2　日期：2026-08-13
> 定位：开发的**主参考文档**。逐条功能点见配套 `恋爱纪念册-功能清单.xlsx`（63 个功能点，表头：模块/功能/功能点/描述/备注，备注已标注机制）；本文补齐架构、数据模型、接口契约与 3 个复杂模块的详细设计。
> 目标：可「添加到主屏幕」的 iOS PWA，数据上云（Supabase），设计从简、偏暖。
> 核心产品模型：**单人模式是基准；两人模式 = 单人模式 + 一层「对方的外来数据」**（a、b 平等；未配对可单人用，配对后选择性同步/共享）。详见 §0。

---

## 0. 核心产品模型（必读）

> **恋爱纪念册的两人模式 = 单人模式 + 一层「对方的外来数据」。**

未配对是系统的**基准态**：每个用户以「自己的数据」为中心，所有功能在单人下都可用（仅聊天需要对方在场）。配对后**不引入第二套体验**，唯一的变化是：用户视图里多出一层来自对方、且对方授权可见的数据。

### 0.1 数据的四种归属
| 归属 | 含义 | 例子 |
|---|---|---|
| 纯本地 | 不触网，对方完全没有此数据 | 每日情话、主题 |
| 我自己的 | 我创建的数据，始终归我 | 一切我建的记录 |
| 外来·仅看 | 对方的复制类数据进我视图，我**不能改** | 对方的日记、影评、打卡状态 |
| 外来·可改 | 对方的同步类数据进我视图，我**能改能删** | 对方的计划、任务、纪念日、影视条目 |

### 0.2 与三机制的关系
- 「外来·仅看」= **复制（replicate）**：进我视图、带创建者标签、不去重，但我只能动自己那份。
- 「外来·可改」= **同步（sync）**：进我视图、带创建者标签、不去重，且我对对方那份也有改/删权（见 §5）。
- 「纯本地 / 我自己的」不跨设备、不进对方视图。
- **唯一例外**：聊天不属于「外来数据层」模型，它是双向实时通道、需对方在线，不能套用「单人 + 外来」表述（见 §13.2）。

### 0.3 这个模型的好处
1. **未配对＝没有外来数据层**，所有功能自然降级为纯单人，无需专门的「离线/单机分支」。
2. **开发实现高度统一**：任何页面 = `我的数据 ∪ 对方授权可见的数据`，差异只在对方那条是否开放编辑——UI 只需在对方数据上加「由 TA 创建」标签并决定是否给编辑入口，不必为双人写第二套逻辑。

## 1. 产品定位
一款面向**情侣二人**的移动端 PWA。强调「温度」而非功能堆砌。
- 无原生推送（已明确放弃提醒功能）。
- 数据上云（Supabase Postgres + Storage），实时同步/可见，多设备一致。
- **两态模型**：未配对 = 单人模式（自己的空间，功能基本可用，仅聊天不可用）；已配对 = 对等双人（选择性同步/共享）。

## 2. 目标用户与使用场景
- 情侣两人，均在大陆，**可科学上网**（Supabase 官方托管 `supabase.co` 可达）。
- 入口：iOS Safari / 微信内置浏览器 → 「添加到主屏幕」。
- 典型：一人先装、先记录（日记/纪念日/计划…），之后拉对方配对，两人的数据在共享空间里各显示各的、互不覆盖。

## 3. 设计原则
- **简单**：底部仅 4 个 Tab；每页只做一个主操作，表单极简。
- **暖**：粉系主色、圆角卡片、手写感字体、柔和微交互。
- **真实优先**：用两人真实照片与素材，避免占位图。
- **拟人化文案**：把「添加」改成「记一笔 / 写下来 / 加一个愿望」。

## 4. 信息架构与导航
底部 **4 个 Tab**：`首页 · 记忆 · 聊天 · 我的`

| Tab | 角色 | 包含 |
|---|---|---|
| 首页 | 情感中枢 | 在一起天数、倒计时、今日情话、今日速览、回忆入口 |
| 记忆 | 共同经历 | 情侣日记、日记日历、照片墙、回忆时间轴 |
| 聊天 | 实时连接 | 即时聊天、在线/输入中/已读（**需配对**） |
| 我的 | 管理入口 | 身份配对、生活（纪念日/计划/任务/打卡）、共娱（影视）、设置（主题/备份/关于） |

> 其余功能全部作为入口收进「我的」页，底部永不增加 Tab。

---

## 5. 同步与权限机制（核心）

### 5.1 三种机制定义
| 机制 | 含义 | 对方可见？ | 对方可改/删？ | 涉及功能 |
|---|---|---|---|---|
| **复制（replicate，不合并）** | 各自数据进入对方视图，带 `owner` 标签，**不去重**（两人各建则各显示一条） | ✅ 可见 | ❌ 仅自己改/删自己的 | 情侣日记、影评、日常打卡（状态） |
| **同步（sync，共享）** | 同一共享空间内，双方都可见、**都可改/删**，带 `owner` 标签、不去重 | ✅ 可见 | ✅ 双方均可改/删 | 影视条目、未来计划、日常任务、纪念日 |
| **本地（local）** | 不触网/不跨设备 | — | — | 每日情话（静态）、主题 |

> 关键澄清：**「复制」与「同步」在可见性上一致（都进对方视图、都不去重、都带创建者标签），区别只在「对方能否改/删」**。复制类仅 owner 可改；同步类双方都可改。

### 5.2 修改/删除权限开放表
- **开放给对方改/删**（同步类）：影视条目、未来计划、日常任务、纪念日（含对方创建的）。
- **不开放**（仅自己改/删自己的，复制类）：情侣日记、影评、日常打卡。
- 聊天：必须配对，双向，发送者自己可删自己的消息。

### 5.3 典型场景说明
- **计划**：A 写「今天买点菜」→ B 看到并改成「今天买点菜和肉」→ A 也看到「今天买点菜和肉」（共享一份、B 改的是同一条）。
- **纪念日**：A 增「某某纪念日 2/24」，B 也增「同名 2/24」→ 两人都看到**两条**，各带「谁创建」标签，且两人对这两条都能改/删（不去重）。
- **日记**：A、B 各写自己的，互相可见但**不能改对方日记**。
- **影视**：同一部电影按外部唯一标识**去重**为一份（客观外来数据）；A、B 各自写**影评**（评分/观后感），互见不可改。
- **在一起天数** = 固定纪念日（type=`together`），默认 `is_private=true`（仅自己可见）；其余普通纪念日默认 `is_private=false`（共享）。

---

## 6. 功能清单索引（详见 xlsx）
完整 63 个功能点见 `恋爱纪念册-功能清单.xlsx`（备注列已标注机制）。模块 → 表 → 机制：

| 模块 | 功能 | 主要表 | 机制 |
|---|---|---|---|
| 首页 | 天数/倒计时/情话/速览/入口 | anniversaries, tasks, checkins, 静态文案 | 聚合只读 |
| 记忆 | 情侣日记/日历/照片墙/时间轴 | diary_entries, diary_photos, 视图 | 复制 |
| 聊天 | 即时聊天/状态 | messages, Storage | 需配对·双向 |
| 我的 | 身份配对 | profiles, couples | 建立关系 |
| 我的 | 纪念日/计划/任务 | anniversaries/plans/tasks | 同步 |
| 我的 | 日常打卡 | checkins | 复制（看状态） |
| 我的 | 影视记录 | movies(条目)+movie_reviews(影评) | 条目同步·影评复制 |
| 我的 | 主题/备份/关于 | 本地 | 本地 |

---

## 7. 重点页面布局要点
### 首页
- Hero：合照 + 「我们在一起 N 天」+ 起始日期（大字号）。
- 纪念日倒计时：粉色卡片。
- 今日情话：白卡 + 手写体，可「换一句」（静态随机，不同步）。
- 今日速览：今日任务 + 打卡状态。
- 最新回忆入口：跳日记/照片/时间轴。

### 记忆
- 情侣日记：时间倒序；双作者用不同色点与头像区分（来自 `profiles.color`）。
- 日记日历：月历标记有日记的日期（小粉点）。
- 照片墙：**仅自动聚合日记中的图片**，不提供主动上传。
- 回忆时间轴：竖向时间轴，聚合各源事件。

### 聊天
- 消息流（文+图）+ 输入框；在线小绿点、对方「输入中…」、已读标记（打开会话才算）。
- 进页先拉历史最近 N 条，再订阅增量。

### 我的（分组）
- 身份：身份配对。
- 生活：纪念日、未来计划、日常任务、日常打卡。
- 共娱：影视记录。
- 设置：主题、备份导出、关于我们。

---

## 8. 技术栈
- 纯前端，`<script>` 直引，**无构建框架**。
- Supabase JS（CDN）。
- 认证：Supabase Auth **匿名登录**，每设备一个 auth user，profile 绑定 couple。
- 图片：Supabase Storage（`avatars/`、`diary/`、`chat/`）。
- PWA：`manifest.json` + Service Worker（缓存应用壳 + 离线兜底）。

---

## 9. 数据模型（精确表结构 + RLS）

### 9.1 两态架构与空间模型
- 所有业务表带 `couple_id`（所属空间）+ `owner_id`（创建者）。
- **未配对**：用户有 1 个 `couples` 行（solo 空间，`pair_code=null`），其 `profiles.couple_id` 指向它 → 只能看到自己。
- **配对**：B 输入 A 的 `pair_code` 后，把 B 所有 solo 记录的 `couple_id` 改为 A 的 shared id（owner 不变），双方指向同一空间 → 立即互见、不去重。见 §12.1。

### 9.2 表结构
```sql
-- 空间 / 情侣对
create table couples (
  id         uuid primary key default gen_random_uuid(),
  pair_code  text unique,            -- 配对码；solo 空间为 null
  is_paired  boolean default false,
  created_at timestamptz default now()
);

-- 身份（id = auth.uid()）
create table profiles (
  id         uuid primary key default auth.uid(),
  couple_id  uuid references couples(id) not null,
  nickname   text not null,
  avatar_url text,                  -- Storage: avatars/{id}.jpg
  color      text not null,         -- 代表色 hex
  created_at timestamptz default now()
);

-- 纪念日（同步类·双方可改删）
create table anniversaries (
  id         uuid primary key default gen_random_uuid(),
  couple_id  uuid references couples(id) not null,
  owner_id   uuid references profiles(id) not null,
  title      text not null,
  date       date not null,
  is_lunar   boolean default false,
  type       text,                  -- together/生日/初约...
  is_private boolean default false, -- 固定"在一起"项默认 true
  created_at timestamptz default now(),
  is_deleted boolean default false
);

-- 未来计划（同步类）
create table plans (
  id         uuid primary key default gen_random_uuid(),
  couple_id  uuid references couples(id) not null,
  owner_id   uuid references profiles(id) not null,
  title      text not null,
  description text,
  type       text,
  status     text default 'idea',    -- idea/doing/done
  is_private boolean default false,
  created_at timestamptz default now(),
  is_deleted boolean default false
);

-- 日常任务（同步类）
create table tasks (
  id          uuid primary key default gen_random_uuid(),
  couple_id   uuid references couples(id) not null,
  owner_id    uuid references profiles(id) not null,
  title       text not null,
  assignee_id uuid references profiles(id),
  status      text default 'todo',   -- todo/done
  is_private  boolean default false,
  created_at  timestamptz default now(),
  is_deleted  boolean default false
);

-- 日常打卡（复制类·看状态·仅自己改）
create table checkins (
  id           uuid primary key default gen_random_uuid(),
  couple_id    uuid references couples(id) not null,
  owner_id     uuid references profiles(id) not null,
  type         text not null,        -- morning/evening/miss
  date         date default current_date,
  note         text,
  streak_self    int default 1,      -- 个人连续天数
  streak_shared int default 1,      -- 共同连续天数
  created_at   timestamptz default now(),
  unique (couple_id, type, owner_id, date)
);

-- 情侣日记（复制类·仅自己改）
create table diary_entries (
  id         uuid primary key default gen_random_uuid(),
  couple_id  uuid references couples(id) not null,
  owner_id   uuid references profiles(id) not null,
  body       text not null,
  created_at timestamptz default now(),
  is_deleted boolean default false
);

create table diary_photos (
  id         uuid primary key default gen_random_uuid(),
  entry_id   uuid references diary_entries(id) not null,
  url        text not null,         -- Storage: diary/{id}.jpg
  created_at timestamptz default now()
);

-- 影视条目（同步类·去重：按 external_id）
create table movies (
  id            uuid primary key default gen_random_uuid(),
  couple_id     uuid references couples(id) not null,
  external_id   text not null,      -- tmdb id 或 片名+年份（客观唯一标识）
  title         text not null,
  poster        text,
  official_rating numeric(3,1),    -- 官方评分（客观元数据）
  watched       boolean default false,
  created_at    timestamptz default now(),
  is_deleted    boolean default false,
  unique (couple_id, external_id)
);

-- 影评（复制类·按人一份·仅自己改）
create table movie_reviews (
  id         uuid primary key default gen_random_uuid(),
  couple_id  uuid references couples(id) not null,
  owner_id   uuid references profiles(id) not null,
  movie_id   uuid references movies(id) not null,
  rating     numeric(2,1) check (rating between 0 and 5),
  review     text,
  created_at timestamptz default now(),
  unique (couple_id, owner_id, movie_id)
);

-- 聊天（需配对·双向）
create table messages (
  id         uuid primary key default gen_random_uuid(),
  couple_id  uuid references couples(id) not null,
  sender_id  uuid references profiles(id) not null,
  body       text,
  kind       text default 'text',   -- text/image
  image_url  text,
  created_at timestamptz default now(),
  read_at    timestamptz
);

-- 回忆时间轴（视图聚合，不建表）
create view timeline as
  select 'diary' as source, id, couple_id, owner_id, created_at, body as title from diary_entries where not is_deleted
  union all
  select 'anniversary', id, couple_id, owner_id, date::timestamptz, title from anniversaries where not is_deleted
  union all
  select 'movie', id, couple_id, null, created_at, title from movies where not is_deleted
  union all
  select 'plan', id, couple_id, owner_id, created_at, title from plans where not is_deleted
  union all
  select 'checkin', id, couple_id, owner_id, created_at, type from checkins;
```

### 9.3 索引
```sql
create index on profiles (couple_id);
create index on messages (couple_id, created_at desc);
create index on diary_entries (couple_id, created_at desc);
create index on checkins (couple_id, type, owner_id, date);
create index on diary_photos (entry_id);
create index on movie_reviews (couple_id, movie_id);
```

### 9.4 RLS（行级安全，核心）
```sql
-- 辅助函数
create or replace function my_couple() returns uuid language sql stable as $$
  select couple_id from profiles where id = auth.uid()
$$;
create or replace function my_id() returns uuid language sql stable as $$
  select auth.uid()
$$;

-- 通用 SELECT：同空间，且（非私有 或 自己是 owner）
create policy "同空间可见" on anniversaries
  for select using (couple_id = my_couple() and (not is_private or owner_id = my_id()));
-- （其余表同理，复制类/同步类一致；movies/messages 无 is_private 字段，去掉该条件）

-- INSERT：必须写自己的 couple 与 owner
create policy "写自己" on anniversaries
  for insert with check (couple_id = my_couple() and owner_id = my_id());

-- UPDATE/DELETE 按机制区分：
-- 同步类（anniversaries/plans/tasks/movies）：双方都能改/删
create policy "同步类双方可改" on anniversaries
  for update using (couple_id = my_couple()) with check (couple_id = my_couple());
create policy "同步类双方可删" on anniversaries
  for delete using (couple_id = my_couple());

-- 复制类（diary_entries/movie_reviews/checkins）：仅 owner 改/删自己的
create policy "复制类仅自己改" on diary_entries
  for update using (couple_id = my_couple() and owner_id = my_id())
  with check (couple_id = my_couple() and owner_id = my_id());
create policy "复制类仅自己删" on diary_entries
  for delete using (couple_id = my_couple() and owner_id = my_id());

-- messages：仅自己发、自己删；read_at 由接收方打开会话时更新（见 §12.2）
create policy "仅自己发消息" on messages
  for insert with check (couple_id = my_couple() and sender_id = my_id());
create policy "仅自己删消息" on messages
  for delete using (couple_id = my_couple() and sender_id = my_id());
create policy "接收方可写已读" on messages
  for update using (couple_id = my_couple()) with check (couple_id = my_couple());
```

### 9.5 Realtime 发布
```sql
alter publication supabase_realtime add table messages, diary_entries, plans,
  tasks, checkins, anniversaries, movies, movie_reviews;
```

---

## 10. 实时与同步接口契约
```js
const sb = supabase.createClient(URL, ANON_KEY);
```
- **聊天订阅**：见 §12.2。
- **数据可见/同步（通用）**：进入模块即 `select ... where couple_id = eq.{C}`，再对该表 `postgres_changes` 订阅 INSERT/UPDATE/DELETE，按 `couple_id` 过滤，收到即刷新 UI。
- **无推送**：关页面即断，重开重新 `select` 全量最新。

## 11. 模块依赖
```
profiles ──► couples（空间，可 solo / shared）
业务表 ──► couples(couple_id) + profiles(owner_id)
timeline(view) ── 聚合事件表
```
依赖根：`couples` + `profiles`（身份配对最先建）。未配对时功能可用（除聊天），配对后空间合并。

## 12. 页面 → 数据调用速查
| 页面 | 读取 | 写入 |
|---|---|---|
| 首页 | anniversaries(下个/在一起)、tasks(今日)、checkins(今日)、静态情话、最新 diary | 无 |
| 记忆·日记 | diary_entries+diary_photos | 增/改/删自己的 diary |
| 记忆·照片墙 | diary_photos | 只读 |
| 记忆·时间轴 | timeline 视图 | 只读 |
| 聊天 | messages 历史+订阅 | 发消息、写 read_at |
| 我的·各入口 | 对应表 | 按机制（同步类双方可改 / 复制类仅自己） |

---

## 13. 复杂模块详细设计

### 13.1 身份配对（建立关系，非合并两空间）
**前置**：Supabase Auth 匿名登录。

**A 创建空间（未配对起点）**
```
auth.signInAnonymously() → user_A
insert couples(pair_code=null, is_paired=false) → solo_A
insert profiles(id=user_A, couple_id=solo_A, nickname_A, color_A)
```
A 此时即可在 solo_A 内正常使用除聊天外所有功能。

**生成配对码 / B 加入**
```
A 点「创建共享空间」→ update couples set pair_code=<6位随机>, is_paired=false where id=solo_A
  （展示 pair_code 给 B）
B 点「加入 TA」→ auth.signInAnonymously() → user_B
  → insert couples(pair_code=null) → solo_B；insert profiles(user_B, solo_B,...)
  → 输入 pair_code → 查到 shared=solo_A
  → RPC join_couple(shared): 把 B 所有业务记录的 couple_id 从 solo_B 改为 shared，
       profiles(B).couple_id=shared，couples(shared).is_paired=true
```
配对后：A、B 的 `couple_id` 都 = shared，owner_id 各自保留 → 双方立即互见、不去重。

**join_couple RPC（SECURITY DEFINER 示例）**
```sql
create or replace function join_couple(p_code text, p_uid uuid)
returns void language plpgsql security definer as $$
declare shared uuid;
begin
  select id into shared from couples where pair_code = p_code;
  -- 迁移 B 的 solo 记录
  update anniversaries set couple_id = shared where couple_id = (select couple_id from profiles where id = p_uid);
  update plans set couple_id = shared where couple_id = (select couple_id from profiles where id = p_uid);
  update tasks set couple_id = shared where couple_id = (select couple_id from profiles where id = p_uid);
  update checkins set couple_id = shared where couple_id = (select couple_id from profiles where id = p_uid);
  update diary_entries set couple_id = shared where couple_id = (select couple_id from profiles where id = p_uid);
  update movie_reviews set couple_id = shared where couple_id = (select couple_id from profiles where id = p_uid);
  update movies set couple_id = shared where couple_id = (select couple_id from profiles where id = p_uid);
  update profiles set couple_id = shared where id = p_uid;
  update couples set is_paired = true where id = shared;
end; $$;
```

**错误 / 边界**
- `pair_code` 不存在 → 「配对码无效」。
- `pair_code` 已 `is_paired` → 「该空间已配对，请创建新空间」。
- 匿名登录失败（网络/VPN）→ 「请检查网络后重试」。

**首启引导**：`nickname`/`color` 两人各填；「在一起」固定纪念日首次进入若为空，首页引导去配置（默认 `is_private=true`）。

### 13.2 即时聊天（需配对·双向）
**发送**
```js
await sb.from('messages').insert({ couple_id, sender_id: auth.uid(), body, kind:'text' });
// 图片：先传 Storage(chat/)，再 insert kind:'image', image_url
```
**接收**：`postgres_changes` INSERT → 追加消息流。
**历史**：进页 `select * from messages where couple_id=eq.C order by created_at desc limit 50`。
**在线（Presence）**：`channel.track({user_id})` + `presenceState` 显示对方在线。
**输入中（Broadcast）**：`channel.send({type:'broadcast',event:'typing'})`，2s 停顿消失。
**已读**：打开会话批量 `update read_at` where `sender_id != me and read_at is null`。
**退出清理**：离开页 `sb.removeChannel(channel)`（防连接累积）。

### 13.3 数据同步 / 可见性（复制不合并）
- **机制**：Supabase Postgres 即真相源，按 `couple_id` 共享空间；`owner_id` 区分作者；不去重。
- **首次**：`select where couple_id=eq.C and not is_deleted order by created_at desc limit 50`。
- **增量**：对该表 `postgres_changes` 订阅，INSERT/UPDATE/DELETE 对应增/改/隐藏。
- **冲突**：各自 `owner_id` 天然区分；共享对象（如同一部电影）用 `external_id` 唯一键避免重复；删除用 `is_deleted` 软删。
- **离线兜底**：断网展示上次 `select` 缓存（IndexedDB 快照），重连重拉 + 重订阅。

---

## 14. 阶段排期
- **基础**：建表 + RLS + 匿名登录 + 身份配对（§13.1）。
- **打通 4 Tab**：聊天（§13.2）→ 首页聚合 → 记忆 → 我的各入口（按机制实现改/删权限）。
- **稳健增强**：离线兜底、主题、备份导出、关于我们。
- 建议顺序：建表/RLS → 配对 → 聊天 demo → 首页 → 记忆 → 我的。

## 15. 风险与备注
- **依赖 VPN**：双方需科学上网连 `supabase.co`。
- **无推送**：聊天仅页面活跃时实时，已读=打开会话才算。
- **iOS PWA 限制**：无原生通知；Safari 部分 API 受限。
- **微信 webview**：需系统级 VPN 覆盖 webview 流量。
- **照片墙**：仅抓日记图，不主动上传。
- **匿名用户留存**：默认长期保留；「退出空间」可显式删 user。
- **影视去重**：按 `external_id`（客观外来数据），影评按人复制。
- **机制语义**：复制=进对方视图·仅自己改；同步=进对方视图·双方可改。日记/影评/打卡为复制；影视条目/计划/任务/纪念日为同步。
