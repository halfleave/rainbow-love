# 恋爱纪念册 · 同步策略对照表

> 与 `概要设计.md`、`恋爱纪念册-功能清单.xlsx` 配套使用。本文是「机制 → 功能 → 权限」的一页速查。
> 核心原则：**两人模式 = 单人模式 + 一层「对方的外来数据」**（a、b 平等；未配对可单人用，配对后选择性同步/共享；复制不合并、带 owner 标签、不去重）。详见 §0。

---

## 0. 核心模型：单人基准 + 外来数据层
> **两人模式 = 单人模式 + 一层「对方的外来数据」。**

未配对是基准态；配对后唯一变化是视图里多出对方授权可见的数据，不引入第二套体验。
- 外来数据分两类：**仅看**（复制：日记 / 影评 / 打卡状态）与 **可改可删**（同步：计划 / 任务 / 纪念日 / 影视条目）。
- 纯本地（情话 / 主题）与「我自己的」数据不进对方视图。
- 聊天是例外，属双向实时通道，不套用此模型。

## 1. 三种机制

| 机制 | 进对方视图？ | 对方可改/删？ | 去重？ | 数据表 |
|---|---|---|---|---|
| **复制 replicate** | ✅ | ❌ 仅自己改自己的 | 不去重（各建各的都显示） | diary_entries, movie_reviews, checkins |
| **同步 sync** | ✅ | ✅ 双方均可改/删 | 不去重（各建各的都显示） | movies, plans, tasks, anniversaries |
| **本地 local** | — | — | — | 每日情话（静态）、主题 |

> 复制与同步**可见性一致**（都进对方视图、都带 `owner` 标签、都不去重），唯一区别是「对方能否改/删」。

## 2. 功能 → 机制 → 权限 对照

| 功能 | 机制 | 对方可见 | 对方可改/删 | 备注 |
|---|---|---|---|---|
| 情侣日记 | 复制 | ✅ | ❌ | 双作者色点区分 |
| 日记日历 | 复制 | ✅ | ❌ | 标记有日记日期 |
| 照片墙 | 复制 | ✅ | ❌ | 仅聚合日记图，不主动上传 |
| 回忆时间轴 | 聚合视图 | ✅ | ❌ | 随各源 |
| 影评（评分/观后感） | 复制 | ✅ | ❌ | 按人一份（movie_reviews） |
| 影视条目（片名/海报/官方评分/看过） | 同步·去重 | ✅ | ✅ | 按 external_id 去重一份 |
| 未来计划 | 同步 | ✅ | ✅ | 各建各的都显示，带创建者标签 |
| 日常任务 | 同步 | ✅ | ✅ | 指派执行人 |
| 纪念日 | 同步 | ✅ | ✅ | 各建各的都显示；固定"在一起"项默认仅自己 |
| 日常打卡 | 复制 | ✅（看状态） | ❌ | 各自打，看对方状态；连续天数双指标 |
| 即时聊天 | 需配对 | ✅ | 仅自己删自己消息 | 双向实时 |
| 每日情话 | 本地 | — | — | 静态几十条，每日随机 |
| 主题 | 本地 | — | — | 本地偏好 |
| 身份配对 | — | — | — | 建立配对关系（join_couple） |
| 在一起天数 | =固定纪念日 | 随该纪念日 | 随该纪念日 | 默认 is_private=true |

## 3. 数据模型要点
- 所有业务表：`couple_id`（空间）+ `owner_id`（创建者）+ `is_deleted`（软删）。
- `couple_id`：未配对 = 个人 solo 空间；配对后统一为 shared（B 的 solo 记录迁移过去，owner 不变）。
- `is_private`：默认 false；固定"在一起"纪念日默认 true（仅自己可见）。
- `movies.external_id` 唯一键（客观外来数据去重）；`movie_reviews` 按 (couple_id, owner_id, movie_id) 每人一份。

## 4. RLS 速查
- **SELECT**：`couple_id = my_couple()` 且（`is_private=false` 或 `owner_id = my_id()`）。
- **INSERT**：`couple_id = my_couple() and owner_id = my_id()`。
- **同步类 UPDATE/DELETE**：`couple_id = my_couple()`（双方可改/删）。
- **复制类 UPDATE/DELETE**：`couple_id = my_couple() and owner_id = my_id()`（仅自己）。
- **messages**：仅自己发/删；接收方打开会话可写 `read_at`。

## 5. 配对流程（join_couple）
1. A 匿名登录 → 建 solo 空间 → 生成 `pair_code`。
2. B 匿名登录 → 建自己的 solo 空间 → 输入 A 的 `pair_code`。
3. 调用 `join_couple`：把 B 所有业务记录的 `couple_id` 改为 A 的 shared，B 的 `profiles.couple_id` 同步改，标记 `is_paired=true`。
4. 此后双方 `couple_id` 相同、owner 各异 → 立即互见、不去重、按机制决定改/删权限。

## 6. 一句话给开发
> 数据都在共享空间里，每条带"谁建的"标签；**日记/影评/打卡只能动自己的，计划/任务/纪念日/影视条目两人都能动；影视一部片只有一份，影评每人一份；情话和主题不联网。**
